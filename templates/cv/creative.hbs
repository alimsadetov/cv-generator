<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <style>
        :root {
            --primary-color: #ff6b6b;
            --secondary-color: #4ecdc4;
            --dark-color: #2c3e50;
            --light-color: #f7f7f7;
        }

        body {
            font-family: 'Helvetica Neue', Arial, sans-serif;
            line-height: 1.6;
            margin: 0;
            padding: 0;
            background: var(--light-color);
        }

        .container {
            max-width: 21cm;
            margin: 0 auto;
            background: white;
            box-shadow: 0 0 20px rgba(0,0,0,0.1);
        }

        .header {
            background: var(--dark-color);
            color: white;
            padding: 40px;
            position: relative;
            overflow: hidden;
        }

        .header::after {
            content: '';
            position: absolute;
            bottom: -50px;
            right: -50px;
            width: 100px;
            height: 100px;
            background: var(--primary-color);
            transform: rotate(45deg);
        }

        .name {
            font-size: 3em;
            margin: 0;
            color: var(--primary-color);
        }

        .position {
            font-size: 1.5em;
            color: var(--secondary-color);
            margin: 10px 0;
        }

        .contact-info {
            display: flex;
            gap: 20px;
            margin-top: 20px;
            font-size: 0.9em;
        }

        .main-content {
            padding: 40px;
        }

        .section {
            margin-bottom: 40px;
        }

        .section-title {
            color: var(--dark-color);
            font-size: 1.5em;
            margin-bottom: 20px;
            position: relative;
            padding-left: 20px;
        }

        .section-title::before {
            content: '';
            position: absolute;
            left: 0;
            top: 50%;
            transform: translateY(-50%);
            width: 10px;
            height: 10px;
            background: var(--primary-color);
            border-radius: 50%;
        }

        .experience-item {
            margin-bottom: 30px;
            position: relative;
            padding-left: 20px;
        }

        .experience-item::before {
            content: '';
            position: absolute;
            left: 0;
            top: 0;
            bottom: 0;
            width: 2px;
            background: var(--secondary-color);
        }

        .experience-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 10px;
        }

        .company-name {
            font-weight: bold;
            color: var(--dark-color);
        }

        .date-range {
            color: var(--primary-color);
            font-style: italic;
        }

        .skills-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(150px, 1fr));
            gap: 15px;
        }

        .skill-item {
            background: var(--light-color);
            padding: 10px;
            border-radius: 5px;
            text-align: center;
            position: relative;
            overflow: hidden;
            transition: transform 0.3s;
        }

        .skill-item::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 0;
            width: 100%;
            height: 3px;
            background: var(--primary-color);
            transform: scaleX(0);
            transition: transform 0.3s;
        }

        .skill-item:hover {
            transform: translateY(-3px);
        }

        .skill-item:hover::after {
            transform: scaleX(1);
        }

        .summary {
            background: var(--light-color);
            padding: 20px;
            border-radius: 5px;
            margin-bottom: 30px;
            position: relative;
        }

        .summary::before {
            content: '"';
            position: absolute;
            top: -20px;
            left: 20px;
            font-size: 60px;
            color: var(--primary-color);
            opacity: 0.3;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1 class="name">{{fullName}}</h1>
            <div class="position">{{position}}</div>
            <div class="contact-info">
                <span>📧 {{email}}</span>
                <span>📱 {{phone}}</span>
            </div>
        </div>

        <div class="main-content">
            <div class="summary">
                {{summary}}
            </div>

            <div class="section">
                <h2 class="section-title">Опыт работы</h2>
                {{#each workExperience}}
                <div class="experience-item">
                    <div class="experience-header">
                        <span class="company-name">{{company}} | {{position}}</span>
                        <span class="date-range">{{startDate}} - {{endDate}}</span>
                    </div>
                    <ul>
                        {{#each responsibilities}}
                        <li>{{this}}</li>
                        {{/each}}
                    </ul>
                </div>
                {{/each}}
            </div>

            <div class="section">
                <h2 class="section-title">Образование</h2>
                {{#each education}}
                <div class="experience-item">
                    <div class="experience-header">
                        <span class="company-name">{{institution}}</span>
                        <span class="date-range">{{startDate}} - {{endDate}}</span>
                    </div>
                    <div>{{degree}} в {{specialization}}</div>
                </div>
                {{/each}}
            </div>

            <div class="section">
                <h2 class="section-title">Навыки</h2>
                <div class="skills-grid">
                    {{#each skills}}
                    <div class="skill-item">{{this}}</div>
                    {{/each}}
                </div>
            </div>

            <div class="section">
                <h2 class="section-title">Языки</h2>
                <div class="skills-grid">
                    {{#each languages}}
                    <div class="skill-item">{{this}}</div>
                    {{/each}}
                </div>
            </div>
        </div>
    </div>
</body>
</html> 